# Project instructions

- Keep the listing UI desktop-first and visually precise.
- Keep listing content in `src/data/listing.ts`; do not duplicate it in components.
- Preserve semantic buttons, dialog semantics, keyboard navigation, focus restoration, and visible focus states.
- Use `apply_patch` for manual edits and validate with `npm run lint` and `npm run build`.
